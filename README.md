# Coaching Classes — GitHub Pages

This version is a self-contained static website. No Node.js, npm, Vite, React or build step is required.

## Upload
Upload `index.html` and `.nojekyll` to the repository root, keep Pages set to **Deploy from a branch → main → /(root)**, then save. The page can be opened directly from the GitHub Pages URL.
